import { CustomPhonePipePipe } from './custom-phone-pipe.pipe';

describe('CustomPhonePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPhonePipePipe();
    expect(pipe).toBeTruthy();
  });
});
